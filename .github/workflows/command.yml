on:
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
    # this found if the funcation deprecated
      - run: |
          deprecated=true
          if [[ "$deprecated" == "true" ]]; then
            echo "::warning::Deprecated feature found"
          fi
